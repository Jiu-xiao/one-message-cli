#include <vector>

#include "om.h"

typedef enum {
  OM_CLI_ARG_TOPIC_NAME = 'n',
  OM_CLI_ARG_TOPIC_LENGTH = 'l',
  OM_CLI_ARG_MESSAGE = 'm',
  OM_CLI_ARG_RECV = 'r',
  OM_CLI_ARG_TRANS = 't',
} om_cli_arg_t;
